#include<bits/stdc++.h>
#define ll long long
#define fl(i,a,n) for(int i=a; i<n; i++)
#define sp(n) fixed << setprecision(n)
using namespace std;

int main()
{
    while (1)
    {
        int a, b; 
        cin>>a>>b;
        cout<<"Bitwise Xor of tow number is: "<<(a^b)<<endl;
    }
    return 0;
}
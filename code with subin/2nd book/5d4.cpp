#include<bits/stdc++.h>
#define ll long long
#define fl(i,a,n) for(int i=a; i<n; i++)
#define sp(n) fixed << setprecision(n)
using namespace std;

int main()
{
    while (1)
    {
        int n1, n2;
        cout<<"Enter two number : ";
        cin>>n1>>n2;
        cout<<"The Bitwise (and) of two number is : "<<(n1&n2)<<endl<<endl;
    }
    return 0;
}